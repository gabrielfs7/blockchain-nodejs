version: '3.7'

services: 
  blockchain_nodejs_container:
    build: .
    ports: 
        - 8080:9999
    networks: 
      - blockchain_nodejs_network
    volumes:
      - ./:/usr/app
      - ./node_modules/:/usr/app/node_modules
    command: "npm start"
      
networks:
  blockchain_nodejs_network:
    external: true
